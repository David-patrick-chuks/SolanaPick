# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/solana-pick

# Rate limiting (in ms and requests per window)
RATE_LIMIT_WINDOW=900000  # 15 minutes
RATE_LIMIT_MAX=100        # 100 requests per window per IP

# Server port
PORT=5000 